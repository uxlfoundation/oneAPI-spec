name: CI

on: [push]

jobs:
  build:
    container:
      image: rscohn2/sphinx-tools:latest
    steps:
    - uses: actions/checkout@v1
    - name: Run a multi-line script
      run: |
        make public
